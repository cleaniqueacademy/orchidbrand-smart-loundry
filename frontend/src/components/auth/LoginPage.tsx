import React, { useState } from "react";
import { AlertCircle, ArrowRight, CheckCircle2, Eye, EyeOff, Lock, Mail, Loader2, Sparkles } from "lucide-react";
import { User } from "../../types";
import { InactiveAccountModal } from "../modals/InactiveAccountModal";

interface LoginPageProps { onLoginSuccess: (user: User) => void; }

export const LoginPage: React.FC<LoginPageProps> = ({ onLoginSuccess }) => {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [showPassword, setShowPassword] = useState(false);
  const [loading, setLoading] = useState(false);
  const [errorMessage, setErrorMessage] = useState("");
  const [successMessage, setSuccessMessage] = useState("");
  const [inactiveAccountUser, setInactiveAccountUser] = useState<any | null>(null);

  const executeLogin = async (event?: React.FormEvent) => {
    event?.preventDefault();
    const normalizedEmail = email.trim().toLowerCase();
    if (!normalizedEmail || !password) { setErrorMessage("Masukkan email dan kata sandi untuk melanjutkan."); return; }
    setLoading(true); setErrorMessage(""); setSuccessMessage("");
    try {
      const response = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: normalizedEmail, password }) });
      const data = await response.json();
      if (!response.ok || !data.success) { if ((data.code === "ACCOUNT_INACTIVE" || data.code === "SUBSCRIPTION_EXPIRED") && data.user) setInactiveAccountUser(data.user); setErrorMessage(data.message || "Email atau kata sandi tidak sesuai."); setLoading(false); return; }
      if (data.token) localStorage.setItem("cleanique_token", data.token);
      setSuccessMessage("Berhasil masuk. Menyiapkan ruang kerja Anda…"); window.setTimeout(() => onLoginSuccess(data.user), 350);
    } catch { setErrorMessage("Layanan sedang tidak dapat dijangkau. Coba lagi beberapa saat lagi."); setLoading(false); }
  };

  const goToRegister = () => { const query = window.location.search; window.history.pushState({}, "", `/register${query}`); window.dispatchEvent(new PopStateEvent("popstate")); };

  return <main className="min-h-screen bg-[#071a28] text-white selection:bg-lime-300 selection:text-[#071a28] lg:grid lg:grid-cols-[minmax(360px,0.92fr)_minmax(480px,1.08fr]">
    <section className="relative hidden overflow-hidden bg-gradient-to-br from-[#071a28] via-[#0c3041] to-[#105d55] p-12 lg:flex lg:flex-col lg:justify-between xl:p-16"><div className="pointer-events-none absolute -right-28 top-20 h-80 w-80 rounded-full bg-lime-300/15 blur-3xl" /><div className="relative z-10"><img src="/laundry-cleanique-outline.png" alt="Laundry Cleanique" className="h-24 w-auto object-contain object-left" /></div><div className="relative z-10 max-w-xl pb-8"><p className="mb-5 inline-flex items-center gap-2 rounded-full border border-lime-200/30 bg-white/10 px-3 py-1.5 text-xs font-semibold text-lime-100"><Sparkles className="h-3.5 w-3.5" /> Operasional lebih rapi setiap hari</p><h1 className="text-4xl font-semibold leading-tight tracking-tight xl:text-5xl">Semua cucian terpantau. Semua pelanggan terlayani.</h1><p className="mt-5 max-w-md text-sm leading-7 text-slate-200/80">Kelola transaksi, pelanggan, laporan, dan notifikasi outlet dari satu ruang kerja.</p><div className="mt-8 grid gap-3 text-sm text-slate-100/90"><p className="flex items-center gap-3"><CheckCircle2 className="h-5 w-5 text-lime-300" /> Kasir dan status pesanan dalam satu layar</p><p className="flex items-center gap-3"><CheckCircle2 className="h-5 w-5 text-lime-300" /> Laporan usaha siap dibaca kapan saja</p><p className="flex items-center gap-3"><CheckCircle2 className="h-5 w-5 text-lime-300" /> Trial sesuai paket yang aktif</p></div></div><p className="relative z-10 text-xs text-slate-300/60">Laundry Cleanique · Smart laundry management</p></section>
    <section className="flex min-h-screen items-center justify-center bg-[#f7faf9] px-5 py-10 text-slate-900 sm:px-10"><div className="w-full max-w-md"><div className="mb-8 lg:hidden"><img src="/laundry-cleanique.png" alt="Laundry Cleanique" className="h-16 w-auto object-contain object-left" /></div><div className="mb-8"><p className="mb-2 text-sm font-semibold text-emerald-700">Selamat datang kembali</p><h2 className="text-3xl font-semibold tracking-tight text-[#102c36]">Masuk ke ruang kerja Anda</h2><p className="mt-2 text-sm leading-6 text-slate-500">Gunakan akun pemilik atau staf outlet untuk melanjutkan operasional.</p></div>{errorMessage && <div role="alert" className="mb-5 flex gap-2.5 rounded-xl border border-rose-200 bg-rose-50 p-3.5 text-sm text-rose-700"><AlertCircle className="mt-0.5 h-4 w-4 shrink-0" /><span>{errorMessage}</span></div>}{successMessage && <div role="status" className="mb-5 flex gap-2.5 rounded-xl border border-emerald-200 bg-emerald-50 p-3.5 text-sm text-emerald-700"><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0" /><span>{successMessage}</span></div>}<form onSubmit={executeLogin} className="space-y-5 rounded-3xl border border-slate-200 bg-white p-6 shadow-[0_20px_60px_rgba(16,44,54,0.08)] sm:p-8"><label className="block text-sm font-medium text-slate-700">Email<div className="relative mt-2"><Mail className="absolute left-3.5 top-1/2 h-4 w-4 -translate-y-1/2 text-emerald-700" /><input value={email} onChange={(e) => setEmail(e.target.value)} type="email" autoComplete="email" required disabled={loading} placeholder="nama@outlet.com" className="w-full rounded-xl border border-slate-200 bg-slate-50 py-3 pl-10 pr-3 text-sm outline-none focus:border-emerald-600 focus:ring-4 focus:ring-emerald-600/10 disabled:opacity-60" /></div></label><label className="block text-sm font-medium text-slate-700">Kata sandi<div className="relative mt-2"><Lock className="absolute left-3.5 top-1/2 h-4 w-4 -translate-y-1/2 text-emerald-700" /><input value={password} onChange={(e) => setPassword(e.target.value)} type={showPassword ? "text" : "password"} autoComplete="current-password" required disabled={loading} placeholder="Masukkan kata sandi" className="w-full rounded-xl border border-slate-200 bg-slate-50 py-3 pl-10 pr-11 text-sm outline-none focus:border-emerald-600 focus:ring-4 focus:ring-emerald-600/10 disabled:opacity-60" /><button type="button" aria-label={showPassword ? "Sembunyikan kata sandi" : "Tampilkan kata sandi"} onClick={() => setShowPassword((value) => !value)} className="absolute right-3.5 top-1/2 -translate-y-1/2 text-slate-400 hover:text-emerald-700">{showPassword ? <EyeOff className="h-4 w-4" /> : <Eye className="h-4 w-4" />}</button></div></label><button type="submit" disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-[#0e6b5b] py-3.5 text-sm font-semibold text-white shadow-lg shadow-emerald-900/15 hover:bg-[#0a574a] disabled:cursor-not-allowed disabled:opacity-60">{loading ? <><Loader2 className="h-4 w-4 animate-spin" /> Memvalidasi akun…</> : <>Masuk ke dashboard <ArrowRight className="h-4 w-4" /></>}</button><div className="border-t border-slate-100 pt-5 text-center text-sm text-slate-500">Belum punya akun? <button type="button" onClick={goToRegister} className="font-semibold text-emerald-700 underline-offset-4 hover:underline">Mulai trial gratis</button></div></form><p className="mt-6 text-center text-xs leading-5 text-slate-400">Akses Anda dilindungi oleh autentikasi akun dan pembatasan sesuai peran outlet.</p></div></section><InactiveAccountModal isOpen={!!inactiveAccountUser} user={inactiveAccountUser} onClose={() => setInactiveAccountUser(null)} isDismissable /></main>;
};
